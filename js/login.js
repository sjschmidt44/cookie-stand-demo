(function() {
  var login = document.getElementById('login');
  login.addEventListener('click', function(event) {
    //on click, present the user with popup
    //user will log in.
    //user will receive second popup
    //user will select from stylesheet or sales 
    //user will be redirected to selected page
  })
})()
